{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AquaTrade AI Webhook Payload",
  "description": "Schema KHỚP §3 EXHAUSTIVE_API_CONTRACT.md (AI -> BE). Sprint 3.1 thêm `originalVideoHash` optional cho audit/dispute.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "ticketId",
    "orderId",
    "fishCount",
    "healthScore",
    "resultVideoUrl",
    "timestamp"
  ],
  "properties": {
    "ticketId": {
      "type": "string",
      "description": "Unique ticket ID generated by AI service."
    },
    "orderId": {
      "type": "string",
      "description": "Order UUID from core-backend."
    },
    "fishCount": {
      "type": "integer",
      "minimum": 0
    },
    "healthScore": {
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "resultVideoUrl": {
      "type": "string"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "originalVideoHash": {
      "type": ["string", "null"],
      "description": "SHA-256 hex digest of the original video AI downloaded. Optional, recommended for dispute/legal audit."
    }
  }
}
